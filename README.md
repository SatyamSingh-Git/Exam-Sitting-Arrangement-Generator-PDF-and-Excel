# School Seating Arrangement (Frontend)

Quick local setup (Node.js and npm required):

1. Install dependencies

```bash
npm install
```

2. Run development server

```bash
npm run dev
```

Open the printed `localhost` URL. The app is a client-side React + Vite project. Use the wizard to add classes, set room layout, choose a pattern, preview, and export PDF/Excel.
